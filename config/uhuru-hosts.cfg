define host{
    use                     generic-host            ; Name of host template to use
    host_name               dea3
    alias                   Linux DEA 3
    address                 199.16.201.151
}


define command{
    command_name            get_cf_metric
    command_line            /home/vladi/Desktop/code/private-uhuru-dashboard/bin/get-metric.sh $ARG1$ $ARG2$ $ARG3$ $ARG4$ $ARG5$ $ARG6$   ;os ip component metric warn critical user password
}

define service{
    name				            cf-service 		    ; The name of this service template
    use				                generic-service		; Inherit default values from the generic-service definition
    max_check_attempts              1			        ; Re-check the service up to 4 times in order to determine its final (hard) state
    normal_check_interval           1			        ; Check the service every 5 minutes under normal conditions
    retry_check_interval            1			        ; Re-check the service every minute until a hard state can be determined
    register                        0       		    ; DONT REGISTER THIS DEFINITION - ITS NOT A REAL SERVICE, JUST A TEMPLATE!
}

define service {
    use						cf-service
    host_name               dea3
    service_description		CPU Usage
    check_command			get_cf_metric!linux!$HOSTADDRESS$!dea!cpu!80!90!root!password1234\!
}